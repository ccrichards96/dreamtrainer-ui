/** Types shared across partner dashboard features (applicants, offers, cohorts). */

export interface Course {
  id: string;
  name: string;
}

export interface Cohort {
  id: string;
  name: string;
}
